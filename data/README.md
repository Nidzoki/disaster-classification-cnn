### Datasets directory
Here is example dataset - [disaster_dataset_example](disaster_dataset_example)

In order to train models, it is necessary to populate dataset directories with respective images and check the load path in notebooks, if it doesn't match the directory name, it won't work.